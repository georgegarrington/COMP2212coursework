while(streams[0].notEmpty() && streams[1].notEmpty() && streams[2].notEmpty()){

    a = streams[2].take(); --C1
    b = streams[1].take(); --B1
    c = streams[0].take(); --A1
    d = b + c; --B1 + A1
    e = a + b; --B1 + C1 

    printAll(a, b, c, d, e);

}; 

end;

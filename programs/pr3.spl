print(0);

while(!streams[1].empty){

    streams[1].drop(1);
    print(streams[1].take, streams[0].take);
    
};

end;

/*

print(0);

while(!streams[1].empty){

    drop does same as "drop 1" in Haskell removes the head of the list

    streams[1].drop;
    print(streams[1].take, streams[0].take);

    you can see expressions are separated by semicolons
    
};

end;

*/